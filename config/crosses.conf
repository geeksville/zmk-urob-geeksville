# Display is left to each shield to decide:
#  - crosses_left enables its OLED (CONFIG_ZMK_DISPLAY=y)
#  - crosses_right has no display (defaults off)
#  - crosses_dongle turns it on via the underdongle_adapter Kconfig.defconfig
CONFIG_PM_DEVICE=y